import { IconDefinition } from "@fortawesome/fontawesome-svg-core";

export interface data{
    video: string;
    h1: string;
    button: string;
    vector: {
        icon: IconDefinition;
        text: string;
    }[];
}